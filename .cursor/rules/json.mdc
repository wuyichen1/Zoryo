---
alwaysApply: true
---
# Flutter ↔ H5 Communication Rules

## 🚨 CRITICAL
- Communication protocol is STRICT and IMMUTABLE
- NEVER invent, rename, remove, or guess any field
- If information is missing, ASK instead of assuming

---

## 1️⃣ Global Message Envelope (REQUIRED)

All messages MUST use the following JSON structure:

### window.userJson：当前用户对象json

```Dart
{
    userId: 'u1', //用户ID（String）
    email: 'test@gmail.com', //用户邮箱（String）
    password: '', //用户密码（String）
    avator: '', //用户头像（String）
    name: 'laiyla1', //用户名称（String）
    about: '这个人很棒', //用户简介（String）
    coins: 0, //用户金币数（int）
    isguest: 0, //是否游客：1 是，0 否（int）
    follow: [], //用户关注IDList（List<String>）
    fans: [], //用户粉丝IDList（List<String>）
    blockList: [], //用户黑名单IDList（List<String>）
    picPostLikeIds: [], //用户喜欢图片帖子IDList（List<String>）
    videoPostLikeIds: [] //用户喜欢视频帖子IDList（List<String>）
 }
```

### window.dynamicJson：帖子对象List json

```Dart
[
    {
        dynamicId: '1', // 帖子ID（String）
        userId: 'u2', // 发布人ID（String）
        dynamicType: 0, //帖子类型（0--图片帖子，1--视频帖子）（int）
        dynamicDesc:
          'xxxxx', //帖子描述
        dynamicTitleType: 0, // 图片帖子类型下标（int）
        dynamicPic: ['', ''], //图片地址List（List<String>）
        dynamicVideo: '', //视频地址（String）
        dynamicLikeCount: 2, //点赞数量（int）
        dynamicCommentCount: 0 //评论数（int）
    }
]
```

### window.userListJson：用户对象List json

```Dart
[
    {
        userId: 'u22', //用户ID（String）
        email: 'test@gmail.com', //用户邮箱（String）
        password: '', //用户密码（String）
        avator: '', //用户头像（String）
        name: 'laiyla1', //用户名称（String）
        about: 'xxxxx', //用户简介（String）
        coins: 0, //用户金币数（int）
        isguest: 0, //是否游客：1 是，0 否（int）
        follow: [], //用户关注IDList（List<String>）
        fans: [], //用户粉丝IDList（List<String>）
        blockList: [], //用户黑名单IDList（List<String>）
        picPostLikeIds: [], //用户喜欢图片帖子IDList（List<String>）
        videoPostLikeIds: [] //用户喜欢视频帖子IDList（List<String>）
     }
 ]
```

### H5 → App：`toLogin`（去登录）

H5 在用户需要转为正式登录时调用（需在 `flutter_inappwebview` 可用后执行）：

```javascript
window.flutter_inappwebview.callHandler('toLogin');
```

App 将导航至登录表单路由：`/auth/form?HvMpj4MMZYUNZDuU=login`。

**说明**：`window.userJson.isguest === 1` 表示当前为游客会话；正式账号登录成功后应为 `0`。游客会话下仍可打开登录页，不会被「已登录访问 /auth 即回首页」的重定向规则拦截。

### H5 → App：`newUserData`（读草稿 / 提交完善并结束注册）

**两阶段注册**：App 内 Sign up 仅创建草稿并进入 H5 完善页，此时**尚未**写入「已登录」；只有在本桥接**提交分支**成功后才会登录并进入首页。

**1）只读（无参数）** — 返回当前草稿的 `name`、`avator`（空头像时为默认图经 CDN 解析后的地址，与 `window.userJson` 规则一致）：

```javascript
const data = await window.flutter_inappwebview.callHandler('newUserData');
// data: { name: String, avator: String }
```

**2）提交最终资料（传入对象）** — App 更新用户、**此时才**标记已登录并持久化，再 `go('/home')`。须在「待完善资料」流程内，否则返回 `{ ok: false }`。

```javascript
const result = await window.flutter_inappwebview.callHandler('newUserData', {
  name: '展示昵称',
  avator: 'https://...' // 可省略或空字符串，空则使用默认本地头像 assets 路径
});
// result: { ok: true } 或 { ok: false }
```

用户在完善页关闭 WebView（`close`）且未完成时，App 会撤销草稿账号并回到可重新注册的状态。

### window.commentJson：评论对象List json

```Dart
[
    {
        dynamicId: '1', //评论ID（String）
        commentId: 'c3', //发布评论相关的帖子ID（String）
        userId: 'u2', //发布评论用户ID（String）
        content: 'xxxxxxx' //评论文案（String）
    }
]
```

### window.chatListJson：聊天室对象List json

```Dart
[
      {
        chatId: 'c1', //聊天室ID（String）
        chatUserIds: ['u1', 'u2'], //聊天室用户IDList（List<String>）
        lastSendContent: 'hello', //最后发送消息文案（String）
        lastSendTime: '2025-05-01 12:00:00', //最后发送消息时间（String）
        unreadMsgCount: 1, //未读消息数量（int）
        lastSendUserId: 'u22' //最后发送消息人ID（String）
      }
]
```

### window.messageListJson：聊天消息对象List json

```Dart
[
      {
        msgId: 'm1', //消息ID（String）
        chatId: 'c1', //聊天室ID（String）
        userId: 'u99', //发送消息用户ID（String）
        sendContent: 'hello', //发送消息文案（String）
        sendPicUrl: '', //发送图片地址（String）
        sendTime: '2025-09-01 10:00:00' //发送时间（String）
      }
]
```

### window.coinListJson：金币对象List json

```Dart
[
      {
        key: 'xxxxxxxx', //金币ID key（String）
        cions: 400, //金币数量（int）
        meney: 1.99 //金币金额（double）
      }
]
```

### window.reportListJson：举报内容对象List json

```Dart
[
      {
        reportContext: 'xx1' //选项内容文案（String）
      }
]
```

### window.publishImageListJson：发布帖子选项对象List json

```Dart
[
    { 
        name: 'Hobbies', //帖子选项文案（String）
        value: 0 //下标（int）
    }
]
```

### window.chatBotDesc：Ai模块对象 json

```Dart
{
  title: 'title', //ai标题文案（String）
  content: 'content', //ai内容文案（String）
  points: 100 //ai付费金额（int）
}
```